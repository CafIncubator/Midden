This file exists only so the Git working tree keeps the "SomeFolder.midden" directory, which is
itself named with the .midden suffix. The crawler must not treat a directory as a metadata file
just because its name ends in .midden.
